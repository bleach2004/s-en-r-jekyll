# config.yml

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: "assets/img"
collections:
  - name: "products"
    label: "products"
    folder: "products/"
    fields:
      - { name: Title }
